{
  "name": "testapp",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t testapp .",
    "docker:start": "docker run --rm -it testapp"
  },
  "author": "Steffen André Langnes",
  "license": "UNLICENCED",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
